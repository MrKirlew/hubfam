{
  "name": "familyhub-relay",
  "version": "0.1.0",
  "private": true,
  "description": "FamilyHub first-party relay: per-household Durable Object (SQLite + WebSocket fan-out). Stores ciphertext only.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.90.0"
  }
}
