# Environment variables — NEVER commit credentials
.env
.env.local
.env.production

# Node
node_modules/

# Expo
.expo/

# Native directories — regenerated by prebuild (CNG)
/android
/ios

# Android / native-module build outputs (gradle, CMake, AAR intermediates)
android/app/build/
android/.gradle/
**/build/
**/.gradle/
**/.cxx/

# iOS
ios/Pods/

# OS files
.DS_Store
Thumbs.db

# Google service credentials
droidgoogser.json
webgserv.json
google-services.json

# Dev screenshots (loose files in root)
*.png
!assets/**/*.png

# Play Store signing key
*.keystore
*.jks
play-store-key.json

# === Internal team framework (private — do not commit) ===
PROJECT_SUMMARY_PART*.md
CLAUDE.md
.claude-team/
PROJECT_SUMMARY_P*.md
DECISION_LOG*.md
TECH_DEBT*.md
init.sh
scan.sh
inspect.sh
handoff.sh

# === All root-level docs — not needed for the app to function ===
/*.md

# === Backup and personal agent state ===
*.bak
memory.db
ConfigBuildCred.txt

# === Ad-hoc dev artifacts (UI dumps, tablet XML captures) ===
window_dump*.xml
view.xml

# === Claude Code harness local state (plans, memory, scheduled tasks) ===
.claude/
